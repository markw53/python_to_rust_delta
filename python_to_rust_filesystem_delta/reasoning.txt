The Python repository defines a deterministic filesystem delta tool. The Rust translation must preserve the behavior of snapshot creation, delta computation, and patch application. The tests validate correctness through round-trip comparisons, metadata changes, symlink handling, and deep directory structures. The Rust implementation should use standard filesystem APIs, strong typing, and deterministic ordering. The model must ensure that all operations map cleanly from Python to Rust while maintaining compatibility with the test suite.
