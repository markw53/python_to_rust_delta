Translate the Python repository into an equivalent Rust implementation.

The repository implements a filesystem delta tool with three major components:

1. Snapshot: walk a directory and record file metadata, hashes, and symlink targets.
2. Delta: compute a patch describing the differences between two snapshots.
3. Apply: apply a patch to mutate a directory to match the target snapshot.

Your Rust translation must:

- preserve all behavior
- preserve all semantics
- preserve all filesystem effects
- preserve deterministic ordering
- preserve patch structure
- pass all tests in /target/tests
- use idiomatic Rust where appropriate
- maintain the same public API shape

Do not change test behavior or expected outputs.

Produce a complete Rust repository in /target that builds with Cargo and passes all tests.
